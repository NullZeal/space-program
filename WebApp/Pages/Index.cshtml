@page
@model IndexModel
@{
    ViewData["Title"] = "Home Page";
    if (Model.Error != null)
    {
        <div class="alert alert-danger">@Model.Error</div>
    }
}

<div class="text-center text-white mt-lg-5">
    <p class="display-6">Welcome to the Home Page!</p>
</div>
